const Navbar = () => {
	return (
		<div className="navbar fixed top-0 bg-primary text-primary-content">
			<button className="btn btn-ghost text-xl">Valentine invitation</button>
		</div>
	);
};

export default Navbar;
